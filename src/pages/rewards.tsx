import * as React from "react"
import type { HeadFC, PageProps } from "gatsby"

const Rewards: React.FC<PageProps> = () => {
  return (
    ''
  )
}

export default Rewards

export const Head: HeadFC = () => <title>Rewards</title>
